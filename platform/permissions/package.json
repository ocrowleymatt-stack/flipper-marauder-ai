{
  "name": "@atlas/platform-permissions",
  "version": "0.0.0",
  "private": true,
  "description": "Capability-based authorisation: evaluates `Grant`s and `CapabilityScope`s against requested actions and resources.",
  "type": "module",
  "exports": {
    ".": {
      "atlas-source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b tsconfig.json",
    "test": "vitest run --root ../.. --project platform platform/permissions"
  }
}
