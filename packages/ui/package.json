{
  "name": "@atlas/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Shared UI components and design tokens used by every app shell.",
  "type": "module",
  "exports": {
    ".": {
      "atlas-source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b tsconfig.json",
    "test": "vitest run --root ../.. --project packages packages/ui"
  }
}
