# Monitoring stub

When HTTP exists, scrape:

- route decisions by alias (Nexus traces)
- provider attempt / retry / fallback counts (execution)
- job status counts
- restore-drill age
- releaseSha vs expected

Do not send prompt bodies to a third-party APM.
