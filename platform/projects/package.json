{
  "name": "@atlas/platform-projects",
  "version": "0.0.0",
  "private": true,
  "description": "Durable project aggregate: project metadata, the objects and revisions a project owns, and their lifecycle.",
  "type": "module",
  "exports": {
    ".": {
      "atlas-source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b tsconfig.json",
    "test": "vitest run --root ../.. --project platform platform/projects"
  }
}
