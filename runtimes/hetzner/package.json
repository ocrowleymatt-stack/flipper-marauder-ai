{
  "name": "@atlas/runtime-hetzner",
  "version": "0.0.0",
  "private": true,
  "description": "Hetzner host runtime adapter: provisioning and deployment target for the platform.",
  "type": "module",
  "exports": {
    ".": {
      "atlas-source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b tsconfig.json",
    "test": "vitest run --root ../.. --project runtimes runtimes/hetzner"
  }
}
