# platform/nexus

Configuration-driven **routing only**. Reads a provider/model registry, returns ordered candidates and a `RouteTrace`.

Does not: retry, stream, `fetch`, or import dungeons.

See `docs/NEXUS-CONTRACT.md`.
