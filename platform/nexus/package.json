{
  "name": "@atlas/platform-nexus",
  "version": "0.0.0",
  "private": true,
  "description": "Thin model router: turns a `RouteRequest` (capability alias or explicit model) into a `RouteDecision` with a full `RouteTrace`, using a model registry and health signals.",
  "type": "module",
  "exports": {
    ".": {
      "atlas-source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b tsconfig.json",
    "test": "vitest run --root ../.. --project platform platform/nexus"
  }
}
