{
  "name": "@atlas/platform-events",
  "version": "0.0.0",
  "private": true,
  "description": "Event backbone: publish, persist, subscribe to and replay `EventEnvelope`s with ordering guarantees.",
  "type": "module",
  "exports": {
    ".": {
      "atlas-source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b tsconfig.json",
    "test": "vitest run --root ../.. --project platform platform/events"
  }
}
