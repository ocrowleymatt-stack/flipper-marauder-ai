# Dungeon: osint

**Boundary:** WHO lookups, dossiers, engine orchestration (SpiderFoot/BigBrother as *runtimes*).
**Forbidden:** Nexus routing on OSINT prompt keywords; weaponized defaults.
**Later sources:** `@ocrowley/osint` jobs + SSE, TheBigBrother as an adapter.
**This phase:** stub only. Default-deny; requires `network.public` and a case id.
