{
  "name": "@atlas/platform-jobs",
  "version": "0.0.0",
  "private": true,
  "description": "Durable jobs: queueing, checkpoints, retry, cancellation and resume for `JobRecord`s, independent of what the job does.",
  "type": "module",
  "exports": {
    ".": {
      "atlas-source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b tsconfig.json",
    "test": "vitest run --root ../.. --project platform platform/jobs"
  }
}
