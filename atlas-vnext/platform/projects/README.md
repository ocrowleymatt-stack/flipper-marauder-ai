# @atlas-vnext/projects

Platform primitive: durable, versioned projects.

Projects are authoritative workspace state. They are not owned by any dungeon. A writing manuscript and an OSINT case are both projects with different `dungeon` labels and manifests.

This package is a design-gate shell: the interface is frozen; the durable store is not implemented in this PR.
