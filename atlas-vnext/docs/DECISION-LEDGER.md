# Atlas vNext decision ledger

Owner Delegate is the decision authority for this programme. Entries are development-scoped and reversible unless marked otherwise.

| ID | Decision | Alternatives | Chosen | Rationale | Evidence | Reversibility | Affected |
|---|---|---|---|---|---|---|---|
| D1 | Treat `b4388c7` / PR #11 as accepted main | Wait for later main; replay superseded Production Readiness heads | Use current main `b4388c7` | Matches expected Production Readiness merge; contracts preserved | `git log -1` | n/a | programme |
| D2 | Add `privacy` dungeon id + `privacy.view\|configure\|audit` capabilities | Reuse only `admin.configure`; UI-only owner flag | New owner-only capabilities | UI hiding is not security; admin.configure is already a tool id | Authority tests; tool catalogue must not advertise privacy.configure | Additive | contracts, permissions, tools |
| D3 | Generic `dungeon_records` + dedicated privacy tables (migration 008) | Per-dungeon tables; JSON file side stores | One record store + privacy policy/audit/proposals | Smallest shared persistence; dungeons stay thin; privacy is tenant-scoped | Existing documents/sites pattern | Forward migration; no DROP | persistence |
| D4 | Effective policy overlays Authority; cannot grant | Policy as only gate; policy in Nexus | Overlay after Authority ALLOW | Behaviour ≠ Authority; policy must not become a grant path | policy.test.ts | Reversible | permissions |
| D5 | Bootstrap host principal is tenant `owner` | Separate owner account; everyone owner | Compose membership role `owner` for host principal | No OIDC yet; local/dev owner must exist for Privacy dungeon | auth membership.role | Reversible | auth, compose |
| D6 | Step-up for consequential policy = CSRF + `CONFIRM` phrase + audit | Password re-auth; skip step-up | Confirm phrase until OIDC exists | Conservative given no password login in this slice | Privacy service | Reversible | privacy |
| D7 | OSINT sockets live in host collector port | Dungeon `node:https`; copy TheBigBrother | Injected `PublicLookupPort` | Architecture forbids dungeon transport; do not vendor scanners | architecture tests | Reversible | osint, host |
| D8 | Investigation reads OSINT findings via record ids, not dungeon imports | Import osint package; duplicate scans | Persistence records + finding ids | Dungeon isolation | architecture dungeon-isolation | Reversible | investigation |
| D9 | Restore Caspa craft as artefacts/jobs/ops, not GoldPipeline | Port GoldPipeline/StoryBible services | Jobs + related dungeon_records + edit/cancel/commission | Census DROP of routers/GoldPipeline; keep behaviour | Caspa parity audit | Reversible | writing |
| D10 | Stacked PRs; human merge of protected main | Force-push main; one mega-PR | Focused `cursor/vnext-*` PRs, merge order documented | Branch protection; no production cutover | GitHub PR settings | n/a | integration |
| D11 | Website preview via host, never Nexus | Nexus static server | Host GET preview from CAS/site revisions | Frozen Website Studio split | WEBSITE-STUDIO.md | Reversible | website, host |
| D12 | Workbench motion/sound are presentation-only | Encode state only in animation | CSS/Web Audio + reduced-motion + captions | A11y; no Nexus/Execution/Authority change | workbench experience | Reversible | apps/web |
| D13 | Sound default off | Default on | localStorage `atlas.sound`, explicit control | Conservative; critical state stays visual | experience.ts | Reversible | apps/web |
| D14 | Research empty file selection retrieves project-wide | Empty = no files like Caspa | omit restrictFileIds when none selected | Research specialist workflow needs retrieval | ResearchService.run | Reversible | research |
| D15 | Combine dungeon HTTP in host `estate.ts` | One router per dungeon file | Single host dispatcher, exclusive dungeon packages | Avoids conflicting compose/server PRs | estate.ts | Reversible | host |
| D16 | Website promote stays `deployment.promote` | Soft UI confirm only | Authority grant + SiteStore retentionClass published | Frozen Website Studio split | website tests | Reversible | website |
| D17 | Privacy owner is bootstrap host principal | Separate owner account | `ownerPrincipalId` + 404 generic deny | No OIDC yet; UI hiding is not security | privacy tests | Reversible | privacy |
| D19 | Website promote consults stored EffectivePolicy | Authority-only promote | Overlay after Authority; default repoWrite=false blocks promote | Privacy controls must bind dungeons | website + estate tests | Reversible | website, osint |
| D20 | Bind stored EffectivePolicy on remaining model/retrieval runs | Leave research/investigation/music/writing Authority-only | Overlay on generate/run/compose/challenge/synthesize; retrievalScope=none skips assemble | Privacy dungeon is not advisory | dungeon + estate tests | Reversible | writing, research, investigation, music, website, osint |
| D21 | Research project-wide retrieval only when policy.retrievalScope=project | Keep D14 always; deny research.run when none | D14 constrained by stored policy; default selected_files uses only selected ids | Security over convenience | research tests | Reversible | research |
| D22 | Workbench Privacy form edits the stored overlay; Website promote explains repoWrite step-up | Network-only form; silent 404 on promote | Presentation-only; server still enforces | Owner must see why promote is blocked | estate.tsx | Reversible | apps/web |
| D23 | Website preview uses sandboxed srcDoc, not framed API URL | Relax XFO/CSP for preview routes | Keep global DENY; fetch HTML and render srcDoc | Independent review: iframe was blocked by frame-ancestors none | estate.tsx | Reversible | apps/web |
| D24 | Proposal decide requires explicit approved\|denied | Missing body defaults to approved | 400 unless status is exact | Conservative; owner-only still must not implicit-approve | estate.ts | Reversible | host |
| D25 | Fold Privacy `package.json` into shared `analyzePackageJson` | Keep PR #17 duplicate FORBIDDEN set | Add `dungeons/privacy/package.json` to the dungeon analyzer list | Owner/Codex P2; one authoritative `TRANSPORT_MODULES` list | architecture estate overlay test | Reversible | architecture |
| D26 | Bind tenant stored EffectivePolicy onto generic Workbench chat | Leave chat client-flag-only; bind every Privacy field in Nexus | Host overlays `toolsEnabled` and `processing/local_only` plus scoped model instructions; viewing stays owner-only | Privacy is not advisory; Nexus provider allow/deny lists stay post-release | workbench + live-tools tests | Reversible | host, privacy |
| D32 | Shared search lives in `platform/search` + host engines, not Nexus or dungeons | Recreate Mountain god-search; embed Brave in Research | Ports `FederatedSearchPort` / `SourceInspectPort`; host injects Brave+SearXNG+Wikipedia; deterministic RRF/canonicalisation | Mountain 5cc7a96 behaviour; dungeons must not fetch | search tests; architecture search.test.ts | Reversible | search, host, research |
| D33 | ConversationRuntime compiles bounded durable history into Execution | Client-side memory; prompt = last turn only | `compileConversationHistory` (24 msgs / 24k chars) passed as `ExecutionContext.history` | ORPHEUS-731 anaphora is a server-side product invariant | history.test.ts; wave1-recovery.test.ts | Reversible | conversation, execution |
| D34 | Dungeon/capability results return via requesting `conversationId` + `postNotice` / claimed turn | Hidden dungeon conversation; provider-success = done | `dungeon_records.conversationId`; research `maybeRunFromConversation`; runtime `workHandler` | IF Atlas says it did something, the user can SEE it in that chat | research tests; wave1-recovery | Reversible | conversation, research |
| D35 | Deep research is a two-wave host-injected loop, synthesis is deterministic in Wave 1 | One LLM call over project files; dump synthesis prompt into the user chat | plan → search → inspect → extract → coverage → second wave → contradictions → report | Mountain runner behaviour without god-service coupling | research second-wave test | Reversible | research |
| D36 | Atlas-first chrome: New Chat / Chats / Projects / Library / Dungeons; Run details remain advanced | Keep Tools/Workbench as the primary IA | Relabel nav only; diagnostics drawer unchanged | Product is Atlas; Workbench is the inspector | App.tsx nav testids | Reversible | apps/web |
| D37 | Conversation research/search/inspect execute as the requesting session principal | Inherit host-owner grants | `sendMessage`/`workHandler`/`ToolEngine` use session principal; missing principal fail-closes | Behaviour ≠ Authority; same-tenant members must not inherit owner `network.public` | wave1-recovery identity tests | Reversible | conversation, host, research |
| D38 | `searchMode` is independent of execution `mode`; web research COMPLETED requires inspected findings | Tie search to live LLM; complete when files exist | `ComposeOptions.searchMode`; web path fails closed if `web.findings.length === 0`; workHandler `failed` marks execution failed | Live acceptance with mock LLM; provider success ≠ user result | wave1b live + research inspect-fail tests | Reversible | host, research, conversation |
| D39 | Only successful inspects with usable body count as research findings; trusted hosts match on label boundaries | Count HTTP errors / snippets as evidence; suffix `endsWith` | Skip `!ok` or empty inspects; `hostname === root \|\| hostname.endsWith('.'+root)` | Result completion; suffix hosts must not inherit confirmed | research inspect-empty + suffix tests | Reversible | research |
| D40 | Work-handler executions publish terminal bus events; routing token estimate includes compiled history; federation timeout aborts engine fetches | SSE-only terminal; estimate current turn; Promise.race without abort | `publish(execution.failed\|completed)`; `estimateTokens(system+content+history)`; `mergeAbort` into engines | Event-bus subscribers and 8K-local routing must see the real request | runtime work-handler + history token tests | Reversible | conversation, search |

